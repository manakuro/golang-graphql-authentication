settings:
  legacy:
    force: false
    interval: 0s
schema:
- name: golang-graphql-authentication
  path: .
  commands:
    run:
      status: true
  watcher:
    extensions:
    - go
    paths:
    - /
    ignore:
      paths:
      - .git
      - .realize
      - vendor
